services:
  app:
    entrypoint:
      - sleep
      - infinity
    image:  docker pull ghcr.io/mentasuave01/deno-devcontainer: latest
    init: true
    volumes:
      - type: bind
        source: /var/run/docker.sock
        target: /var/run/docker.sock
